# Isolate this rewrite project as its own pnpm workspace root,
# so it does not inherit the parent cloudflare-os monorepo's dependencies.
packages:
  - .
allowBuilds:
  '@prisma/client': true
  '@prisma/engines': true
  argon2: true
  cpu-features: true
  prisma: true
  sharp: true
  ssh2: true
